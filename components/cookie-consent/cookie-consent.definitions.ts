export const COOKIE_CONSENT_KEY = '__cookie-consent'

export enum Consent {
  Accepted = 'accepted',
  Declined = 'declined'
}

export enum ConsentLabel {
  Accept = 'Accept',
  Decline = 'Decline'
}
